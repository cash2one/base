
<html>
	<head>
		<link rel="stylesheet" href="{{ STATIC_URL }}bootstrap/css/bootstrap.min.css" />
		<title>Menu</title>
		<meta charset='utf-8>'>
	</head>
	
	<body>
		<h3>Welcome~    {{user_name}}</h3>
		<h2>餐厅列表</h2>
		<form action="/res/menu/" method="get">
		<table class="table table-striped table-condensed">
			<tr>
				<th>选取</th>
				<th>店名</th>
				<th>电话</th>
				<th>地址</th>
				<th>评论</th>
			</tr>
			{% for r in restaurants %}
			<tr>
				<td> <input type="radio" name='id'value="{{r.id}}"></td>
				<td>{{r.name}}</td>
				<td>{{r.phone_number}}</td>
				<td>{{r.address}}</td>
				<td><a href="/res/comment/{{r.id}}/">评论<a></td>
				</tr>
			{%endfor%}
		</table>

		<input type="submit" value=" 查看 Menu" class="btn btn-primary btn-md">
		</form>
	</body>
</html>
